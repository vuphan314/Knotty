function correctSign(x) return
    1 if x > 0 else 0 if x = 0 else -1

function incorrectSign(x) return
    (1 if x > 0 else 0) if x = 0 else -1


check correctResult :=
    correctSign(2)

check incorrectResult :=
    incorrectSign(2)
