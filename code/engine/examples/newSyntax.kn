let c = im / 2

let f1(x) = x^2 + c

let f2(x, y) = f1(x) * f1(im * y)

let s1(x) = sum[i in 0 ... x - 1][i * x] + f2(x, 0)

let s2(x, t) =
    t / im if x = 1 else
    9 + x^2 if t = im / 3 else
    s1(x) * im + f2(t, t^-1)
