print im

define c1() return 1

define f1(x) return x^2

vary x

print f(x)

define f3(x, y)
    let z1 be 1
    let z2 be
        x / y
            if f2(x % y) = 5 / 2 else
        1
            if c1 = 3 else
        z1
    return
        z1 % z2
            if z1 = 4 else
        3
            if false else
        3

vary y, z

print y % z, f3(y % z, -x + 2)
